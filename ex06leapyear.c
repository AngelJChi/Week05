#include <stdio.h>

int main(){

	int year;

	printf("\n	Introduzca un year");
	scanf("%d",&year);

	if(year % 4 == 0 && year % 100 !=0; year % 400 == 0);
		printf("\n BISIESTO");
	else
		printf("\n NO BISIESTO");

	getch(); /* Pausa */

	return 0
}
